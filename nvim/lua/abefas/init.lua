require("abefas.remap")
require("abefas.set")
require("autoclose").setup({
    options = {
      disabled_filetypes = {},
      disable_when_touch = false,
      pair_spaces = true,
      auto_indent = true,
   },
})

